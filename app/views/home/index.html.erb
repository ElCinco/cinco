<% if @list %>
  <%= @list.publish_date %>
  <ul>
    <% @list.links.each do |link| %>
    <li>
      <a href="<%= link.url %>"><%= link.title %></a>
      <span class="category"><%= link.category.label %></span>
    </li>
    <% end %>
  </ul>
<% else %>
  No lists!
<% end %>
