%ul.admin-links
  %li.admin-welcome-header
    Welcome #{current_user.username}
    %span.tilda ~
  %li
    - if @list && @list.links.length >= 5
      list full
    - else
      = link_to 'Create Link', admin_new_link_path, :class => "button-outline"
  %li
    = link_to 'Change Password', edit_user_registration_path, :class => "button-outline"
