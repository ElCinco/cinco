<!DOCTYPE html>
<html>
  <head>
    <title>Cinco</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="<%= Date.today.strftime("%A").downcase %>">
    <div class="user_nav">
      <span class="icon-user-actions">
        <%= inline_svg "icon-gear.svg", class: 'icon-gear' %>
        <ul class="user-nav-items">
          <% if current_user %>
            <li> Logged in as <%= current_user.username %> <span class="tilda">~</span><span class="the-dirtiest-code"></span></li>
            <li> <%= link_to 'Post Something', admin_path %> </li>
            <li> <%= link_to 'Log out', destroy_user_session_path, :method => :delete %> </li>
          <% else %>
            <li><%= link_to 'Log In', admin_path %></span><span class="the-dirtiest-code"></span></li>
          <% end %>
        </ul>
      </span>
    </div>

    <h1 class="site-title"><a href="/">cinco.<span>is</span></a></h1>
    <p class="site-description header-message">5 nice links</p>
    <p class="weekend-message header-message">closed for the weekend</p>

    <p class="notice header-message"><%= notice %></p>
    <p class="alert header-message"><%= alert %></p>
    <div class="content-wrap">
      <%= yield %>
    </div>
  </body>
</html>
